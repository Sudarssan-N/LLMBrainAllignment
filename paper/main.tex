\documentclass{article}

% ---------------------------------------------------------------------------
% NeurIPS 2024 style. Download neurips_2024.sty from https://neurips.cc/Conferences/2024/PaperInformation/StyleFiles
% and place it next to this file. Use [preprint] while drafting; switch to the
% camera-ready/submission option at the venue we finally target.
% ---------------------------------------------------------------------------
\usepackage[preprint]{neurips_2024}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{xcolor}

% Draft note macro — strip before submission.
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}
\newcommand{\prelim}[1]{\textcolor{blue}{#1}}

\title{Disentangling Statistical Surprisal from Hierarchical Structure\\
in LLM--Brain Alignment}

\author{%
  Sudarssan N.\thanks{Correspondence: \texttt{sudarssan73@gmail.com}} \\
  Independent Researcher \\
  \texttt{sudarssan73@gmail.com} \\
  \And
  Jeyaanth A. \\
  Independent Researcher \\
  \texttt{jeyaanth.am@gmail.com} \\
}

\begin{document}

\maketitle

\begin{abstract}
Large language model (LLM) hidden states linearly predict human language-network fMRI
responses at close to the noise ceiling, but it remains contested whether this alignment
reflects shared \emph{linguistic structure} or merely shared sensitivity to next-word
predictability (surprisal). We quantify, on the Pereira et al.\ (2018) benchmark, how much
voxel-level alignment is \emph{uniquely} explained by LLM hidden representations \emph{beyond}
surprisal, using cross-validated ridge encoding combined with variance partitioning
(commonality analysis). Across six models spanning $124$M--$500$M parameters and four model
families, hidden representations uniquely explain more than an order of magnitude more brain
variance than surprisal at the most predictive layer (and up to $\sim$$70\times$ more at
intermediate layers, where surprisal contributes least), with this advantage \emph{growing}
with network depth. Contrasting models, the unique representational contribution tends to
\emph{increase} with scale within a controlled model family (Pythia $160$M$\rightarrow$$410$M),
though across families it is confounded by architecture and training data rather than parameter
count alone. Instruction tuning \emph{significantly reduces} both overall alignment and
unique-beyond-surprisal contribution (non-overlapping $95\%$ confidence intervals). Our results,
obtained with inference-only compute, sharpen the structure-versus-statistics debate into a
quantitative, reproducible encoding-model question.
\end{abstract}

\section{Introduction}
\label{sec:intro}

A central finding of contemporary neuro-AI is that the internal representations of transformer
language models predict neural responses in the human language network with striking accuracy:
\citet{schrimpf2021} report that GPT-2-xl predicts the Pereira2018 and Fedorenko2016 datasets
``at close to 100\% of the noise ceiling,'' with intermediate layers most predictive. As this
``predictivity'' has saturated, attention has shifted from \emph{whether} models align to
\emph{why} they do.

A live debate concerns whether alignment reflects shared linguistic \emph{structure} or shared
sensitivity to next-word \emph{predictability} (surprisal). \citet{kauf2023} argue that
lexical-semantic content, rather than syntactic form, drives ANN--brain similarity; other work
shows that highly capable models can become \emph{less} human-like on prediction-based
behavioural measures \citep{superhuman2025}. Surprisal and structural complexity are, however,
notoriously collinear, so a correlational answer is insufficient.

We take a deliberately quantitative stance. Rather than asking whether structure \emph{or}
surprisal explains alignment, we ask: \textbf{how much of LLM--brain alignment on Pereira2018 is
\emph{uniquely} explained by hidden representations \emph{beyond} surprisal, and how does that
residual depend on model size, layer depth, and instruction tuning?} We answer it with variance
partitioning over a voxel-wise encoding model, which cleanly separates the variance unique to
hidden states, unique to surprisal, and shared between them---robust to their collinearity.

\paragraph{Contributions.}
\begin{itemize}
  \item A reproducible, inference-only pipeline that reproduces the Schrimpf-style
  noise-ceiling-normalized predictivity curve and augments it with explicit surprisal control
  via variance partitioning (Section~\ref{sec:methods}).
  \item Evidence that hidden representations uniquely explain more than an order of magnitude
  more brain variance than surprisal (up to $\sim$$70\times$ at intermediate layers), with the
  gap growing across network depth (Section~\ref{sec:core}).
  \item A model-family comparison showing that unique-beyond-surprisal contribution tends to
  grow with scale within a controlled family but is confounded by architecture across families,
  and that instruction tuning significantly reduces it
  (Sections~\ref{sec:size}--\ref{sec:instruct}).
\end{itemize}

\section{Related Work}
\label{sec:related}

\paragraph{LLM--brain predictivity.} \citet{schrimpf2021} established strong linear predictivity
of language-network responses from transformer activations; follow-up work related predictivity
to next-word prediction performance and to scale. \citet{merlin2022} controlled for changes in
next-word prediction and argued some alignment depends on information beyond it.

\paragraph{What drives alignment.} \citet{kauf2023} perturb word order and content/function
words on Pereira2018, concluding lexical-semantic content dominates; their analysis does not
statistically control for surprisal in the hidden-state-to-brain mapping nor construct
surprisal-matched sets. Joint information-theoretic and syntactic predictors of neural data have
been studied with MEG and naturalistic listening, showing structure-sensitive effects can
survive surprisal controls.

\paragraph{Our position.} We isolate the \emph{unique} voxel-level variance explained by hidden
states beyond surprisal within one encoding framework, and connect the residual to size, depth,
and instruction tuning---a combination not, to our knowledge, previously reported together.

\section{Methods}
\label{sec:methods}

\paragraph{Data.} We use the Pereira et al.\ (2018) language-network fMRI dataset
\citep{pereira2018}, accessed through the \texttt{brain-score/language} harness
\citep{schrimpf2021}. We analyse the $384$-sentence experiment ($9$ subjects). The assembly is
already restricted to language-network functional ROIs. Voxels are subject/experiment specific;
we drop voxels with missing coverage, yielding $384$ sentences $\times\ 12{,}155$ voxels.

\paragraph{Models.} We probe six autoregressive models across four families and a
$3\times$ size range: GPT-2 ($124$M) \citep{radford2019}, OPT-125M \citep{zhang2022opt},
Pythia-160M and Pythia-410M \citep{biderman2023pythia}, and the Qwen2.5-0.5B base/instruct pair
\citep{qwen2025}. All are run inference-only.

\paragraph{Activation extraction and pooling.} For each sentence we extract hidden states from
every layer and pool tokens to a single per-sentence vector using the \emph{last-token}
representation (a causal LM's final token has attended to the full sentence). We verified that
mean pooling collapses layer differences, consistent with a bag-of-words confound.

\paragraph{Surprisal features.} We compute per-token surprisal $-\log_2 P(t \mid t_{<i})$ from
the same model and summarize each sentence by its mean, sum, and final-token surprisal,
yielding a $3$-dimensional predictor.

\paragraph{Encoding model.} For features $X$ (activations or surprisal) and responses $Y$ we fit
$5$-fold cross-validated ridge regression with the regularization strength selected by efficient
leave-one-out generalized cross-validation, reporting per-voxel held-out Pearson $r$.

\paragraph{Noise ceiling.} Because Pereira voxels are subject-specific with no within-subject
repeats, we estimate a \emph{cross-subject} ceiling: each held-out subject's voxels are
predicted from the other subjects' responses to the same sentences (PCA-reduced cross-validated
ridge). Normalized predictivity is $r / \text{ceiling}$, averaged over voxels whose ceiling
exceeds $0.1$. The mean ceiling is $0.269$ ($n=11{,}743$ retained voxels).

\paragraph{Variance partitioning.} Given hidden features $H$ and surprisal features $S$, we fit
encoding models for $H$, $S$, and $H \cup S$ and decompose explained variance:
\begin{align}
\text{unique}(H) &= R^2(H \cup S) - R^2(S), &
\text{unique}(S) &= R^2(H \cup S) - R^2(H), \\
\text{shared} &= R^2(H) + R^2(S) - R^2(H \cup S). & &
\end{align}
We report $95\%$ confidence intervals by bootstrap over voxels.

\section{Results}
\label{sec:results}

\subsection{Reproducing noise-ceiling predictivity}
\label{sec:repro}
With last-token pooling and the cross-subject ceiling, GPT-2 reproduces the Schrimpf-style
result: normalized predictivity rises with depth to $\sim$$1.0$ at the final layer
($0.11 \rightarrow 0.98$). For this $124$M model the peak is at the last layer; the deeper
models (Pythia-410M, Qwen2.5-0.5B) saturate the ceiling several layers before the end
(normalized $r > 1$ from layers $20$ and $22$ respectively), consistent with the reported
``peak moves earlier with scale.'' Normalized values slightly exceeding $1$ reflect the
conservative cross-subject ceiling (Section~\ref{sec:discussion}).

\begin{figure}[t]
\centering
\includegraphics[width=0.6\linewidth]{figures/fig_layercurve.pdf}
\caption{Noise-ceiling-normalized predictivity versus relative layer depth for all six models.
The dashed line marks the cross-subject ceiling ($r/\text{ceiling}=1$). Deeper models saturate
the ceiling before the final layer.}
\label{fig:layercurve}
\end{figure}

\subsection{Hidden representations dominate surprisal}
\label{sec:core}
Variance partitioning shows that hidden representations uniquely explain far more brain variance
than surprisal at every layer, and the advantage grows with depth. For GPT-2, peak
$\text{unique}(H) = 0.052$ ($95\%$ CI $[0.051, 0.053]$) at the final layer versus
$\text{unique}(S) = 0.003$ there, a ratio of $\sim$$17\times$; at intermediate layers, where the
unique surprisal contribution falls to $\sim$$0.0005$, the ratio exceeds $70\times$. The effect
holds across all six models, with peak ratios of $16$--$19\times$ (Table~\ref{tab:sweep},
Figure~\ref{fig:varpart}). The result replicates on the independent $243$-sentence experiment
(GPT-2 peak $\text{unique}(H) = 0.049$, Qwen2.5-0.5B $0.046$).

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_varpart.pdf}
\caption{Variance partitioning per model: voxel variance unique to hidden states (blue), shared
(green), and unique to surprisal (yellow) versus relative depth. The unique-hidden component
dominates and grows with depth; the unique-surprisal sliver remains an order of magnitude smaller
throughout.}
\label{fig:varpart}
\end{figure}

\subsection{Size: unique structure grows with scale within a family}
\label{sec:size}
Normalized predictivity rises with model quality and scale (highest for Qwen2.5-0.5B, $1.13$).
The \emph{unique} contribution of hidden states beyond surprisal tends to rise with scale as
well: within the controlled Pythia family it grows from $0.039$ ($160$M) to $0.057$ ($410$M),
a $45\%$ increase, and the largest model (Qwen2.5-0.5B) attains the highest value ($0.066$).
The relationship is not strictly monotonic \emph{across} families---well-trained small models
(GPT-2 $124$M: $0.052$; OPT-125M: $0.059$) exceed Pythia-160M---indicating that architecture and
training data, not parameter count alone, govern the unique representational contribution. (We
note this corrects an earlier estimate from an unstable ridge that had spuriously depressed
Pythia-410M.) Figure~\ref{fig:sweep} plots peak unique-hidden with $95\%$ confidence intervals
against parameter count.

\begin{figure}[t]
\centering
\includegraphics[width=0.65\linewidth]{figures/fig_sweep.pdf}
\caption{Peak unique-hidden $R^2$ per model with $95\%$ voxel-bootstrap confidence intervals,
ordered by parameter count. Within the Pythia family the contribution rises with scale; across
families the smallest well-trained models (GPT-2, OPT) exceed Pythia-160M.}
\label{fig:sweep}
\end{figure}

\subsection{Instruction tuning significantly reduces alignment}
\label{sec:instruct}
For the Qwen2.5-0.5B base/instruct pair (matched architecture and size), instruction tuning
\emph{reduces} both normalized predictivity ($1.130 \rightarrow 1.103$) and peak unique-hidden
($0.0661 \rightarrow 0.0605$; both peak in the final layers), a $\sim$$8.5\%$ relative reduction.
A paired comparison across the $12{,}137$ shared voxels---each model taken at its own peak
layer---gives a mean difference of $+0.0068$ ($95\%$ bootstrap CI $[+0.0065, +0.0071]$, strictly
positive), confirming the effect is reliable rather than marginal.

\begin{table}[t]
\caption{Model sweep on Pereira2018 ($384$-sentence experiment, last-token pooling,
cross-subject noise ceiling; stabilized LOO-GCV ridge). ``norm $r$'' is best-layer normalized
predictivity; ``unique$(H)$'' is the peak across layers with a $95\%$ voxel-bootstrap CI;
``unique$(S)$'' is at that layer; $H{:}S$ is their ratio at the peak layer.}
\label{tab:sweep}
\centering
\begin{tabular}{lrrrrr}
\toprule
Model & Params & best norm $r$ & peak unique$(H)$ [95\% CI] & unique$(S)$ & $H{:}S$ \\
\midrule
GPT-2            & 124M & 0.98 & 0.052 [.051,.053] & 0.0030 & 17$\times$ \\
OPT-125M         & 125M & 1.01 & 0.059 [.058,.060] & 0.0035 & 17$\times$ \\
Pythia-160M      & 160M & 0.75 & 0.039 [.038,.040] & 0.0025 & 16$\times$ \\
Pythia-410M      & 410M & 1.06 & 0.057 [.056,.058] & 0.0030 & 19$\times$ \\
Qwen2.5-0.5B     & 500M & 1.13 & 0.066 [.065,.067] & 0.0036 & 19$\times$ \\
Qwen2.5-0.5B-Inst& 500M & 1.10 & 0.061 [.060,.061] & 0.0034 & 18$\times$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Representational geometry (RSA cross-check)}
\label{sec:rsa}
As a geometry-level cross-check independent of the linear encoding model, we correlate each
layer's representational dissimilarity matrix (RDM) with the brain RDM (Figure~\ref{fig:rsa}).
RSA scores are small and noisier than the encoding results---peaking around $\rho \approx
0.04$--$0.05$---but are positive for the most predictive models and broadly consistent with the
encoding ordering, indicating the linear-predictivity findings are not an artifact of the
encoding mapping alone.

\begin{figure}[t]
\centering
\includegraphics[width=0.6\linewidth]{figures/fig_rsa.pdf}
\caption{RSA (Spearman $\rho$ between model-layer and brain RDMs) versus relative depth. Scores
are small but positive for the more predictive models, corroborating the encoding-model results
with a mapping-free measure.}
\label{fig:rsa}
\end{figure}

\section{Discussion and Limitations}
\label{sec:discussion}
Our central result---that hidden representations uniquely explain far more brain variance than
surprisal, growing with depth---supports a structural, rather than purely predictive, account of
LLM--brain alignment, while quantifying the (small but non-zero) unique surprisal contribution.

\paragraph{Limitations.} (i) Our main sweep uses the $384$-sentence experiment; the core
hidden-$\gg$-surprisal result replicates on the independent $243$-sentence experiment
(Section~\ref{sec:core}), but the size and instruction-tuning contrasts there await a full
sweep. (ii) Our cross-subject ceiling is principled but not
identical to the brain-score extrapolation ceiling; some normalized values slightly exceed $1$,
indicating a conservative ceiling estimate. (iii) Confidence intervals are bootstrap over
voxels; a stimulus-level bootstrap is stronger and planned. (iv) Encoding pools all
language-network voxels jointly rather than per subject. (v) Surprisal is a low-dimensional
summary; richer information-theoretic and symbolic-syntactic predictors would sharpen the
``structure'' side.

\section{Conclusion}
\label{sec:conclusion}
Framing the structure-versus-statistics debate as a variance-partitioning question yields a
clear, reproducible answer on Pereira2018: alignment is overwhelmingly carried by hidden
representations beyond surprisal; this unique contribution grows with scale within a controlled
model family but is shaped by architecture and training across families; and instruction tuning
significantly reduces it. The full pipeline runs inference-only and is released for reproduction.

\section*{Reproducibility}
Code, configuration, and experiment tracker are available at the project repository. All results
use public models and the public Pereira2018 release via \texttt{brain-score/language}.

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
