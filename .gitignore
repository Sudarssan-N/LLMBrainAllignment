# Derived artifacts (regenerated by the pipeline)
data/derived/
data/raw/
*.npz
*.npy
*.pt
*.ckpt

# HF / torch caches
.cache/
hf_cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
build/
dist/

# Notebooks output (keep the .ipynb, strip heavy outputs manually before commit)
# notebooks/*.ipynb

# LaTeX build artifacts (keep sources: .tex, .bib, .sty)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.pdf
paper/*.synctex.gz

# Overleaf upload bundle (regenerate from paper/)
*.zip

# OS / editor
.DS_Store
.vscode/
.idea/

# Env
.venv/
venv/
env/
*.env
